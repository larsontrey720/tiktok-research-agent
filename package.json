{
  "name": "tiktok-research-agent-api",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "bun run server.ts",
    "dev": "bun run server.ts",
    "build": "echo 'No build step needed'"
  },
  "dependencies": {
    "hono": "^4.0.0"
  }
}
